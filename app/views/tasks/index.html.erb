<h1>Liste des tâches</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
      - <%= task.details %>
      - <%= task.completed ? 'Completed' : 'Not completed' %>
    </li>
  <% end %>
</ul>
